{
  "name": "arduino",
  "version": "1.0.0",
  "description": "",
  "main": "servo.js",
  "scripts": {
    "build": "browserify servo.js -t babelify --outfile bundle.js",
    "watch": "watchify servo.js -t babelify --outfile bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rui Ramos <ruiramos@gmail.com> (http://ruiramos.com/)",
  "license": "ISC",
  "dependencies": {
    "firebase": "^2.2.4",
    "johnny-five": "^0.8.58"
  },
  "devDependencies": {
    "babelify": "^6.0.2"
  }
}
